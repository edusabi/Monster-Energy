<%- include('../partials/head') %>

<div class="voltarPainel">
    <a href="/">< Voltar</a>
</div>

<% if (errorMessages.length > 0) { %>
    <% errorMessages.forEach(function(message) { %>
        <div class="alert-danger" id="error-message"><%= message %></div>
    <% }); %>

    <script>
        function hideSuccessMessages() {
            var errorMessages = document.querySelectorAll('.alert-danger');
            errorMessages.forEach(function(message) {
                message.style.display = 'none';
            });
        }
        setTimeout(hideSuccessMessages, 3500);
    </script>

<% } %>

<% if (successMessages.length > 0) { %>
    <% successMessages.forEach(function(message, index) { %>
        <div class="alert-sucess" data-unique-id="success-message-<%= index %>"><%= message %></div>
    <% }); %>

    <script>
        function hideSuccessMessages() {
            var successMessages = document.querySelectorAll('.alert-sucess');
            successMessages.forEach(function(message) {
                message.style.display = 'none';
            });
        }

        setTimeout(hideSuccessMessages, 3500);
    </script>
<% } %>

<div class="form">
    
    <form action="" method="POST" class="formRegistro">
            
            <h1>Faça seu login</h1>
            
            <div>
                <input type="email" name="email" required class="input" autocomplete="none">
                <label for="">E-mail</label>
            </div>
            
            <div>
                <input type="password" id="inputSenhaLogin" name="password" required class="input">
                <label for="">Senha</label>
                <ion-icon name="eye-outline" id="olhosAbrt3"></ion-icon>
            </div>
        
        <div class="buttonDiv">
            <button type="submit">Entrar</button>
        </div>

        <div>
            <a href="/users/atlSenha"><Nav>Esqueceu a senha? Clique aqui</Nav></a>
        </div>

        <div>
            <a href="/users/registro"><Nav>Não possui conta? Clique aqui</Nav></a>
        </div>
        
    </form>
    
    </div>

<script src="/js/cadastroLogin.js"></script>
